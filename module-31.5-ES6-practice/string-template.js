// 2. string template
// back-ticks ``

const firstName = 'zonaet';
const lastName = 'munna';
const fullName = `${firstName} ${lastName}`;
console.log(fullName);
// back-ticks ``
const multiLine = `it is first line 
it is second line
it is third line`
console.log(multiLine);