const bottle = {
  color: 'blue',
  price: 500,
  isCleand: true,
  hold: 'water',
  
}
// object keys
const keysBottle = Object.keys(bottle);
// console.log(keysBottle);

// object values
const valuesBottle = Object.values(bottle);
// console.log(valuesBottle);

const entityBottle = Object.entries(bottle);
console.log(entityBottle);
